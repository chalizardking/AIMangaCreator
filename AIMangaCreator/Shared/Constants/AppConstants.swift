import Foundation

struct Config {
    // In a real app, these would be loaded from Info.plist or environment variables
    static let openAIKey = "sk-placeholder"
    static let geminiKey = "gemini-placeholder"
    static let openRouterKey = "sk-or-placeholder"
}

struct AppConstants {
    static let appName = "AI Manga Creator"
}
